---
interface Props {
  lastUpdated: string;
}

const { lastUpdated } = Astro.props;
---

<header class="header">
  <div class="header-content">
    <h1>Infos in und um Pöcking</h1>
  </div>
</header>

<style>
  .header {
    padding: 48px 16px 32px;
  }

  @media (min-width: 640px) {
    .header {
      padding: 64px 24px 40px;
    }
  }

  .header-content {
    max-width: 1200px;
    margin: 0 auto;
  }

  h1 {
    font-size: 32px;
    font-weight: 400;
    color: var(--color-text);
    letter-spacing: -0.02em;
    line-height: 1.2;
    margin-bottom: 12px;
  }

  @media (min-width: 640px) {
    h1 {
      font-size: 48px;
    }
  }

  .stats {
    font-size: 14px;
    color: var(--color-text-secondary);
  }

  .stats strong {
    color: var(--color-text);
  }
</style>
