---
interface Props {
  lastUpdated: string;
}

const { lastUpdated } = Astro.props;

function formatDate(dateStr: string): string {
  return new Date(dateStr).toLocaleDateString('de-DE', {
    day: 'numeric',
    month: 'long',
    year: 'numeric'
  });
}
---

<header class="header">
  <div class="header-content">
    <div class="header-main">
      <h1>
        <span class="logo">🏔️</span>
        <span class="title">Raus ins Voralpenland</span>
      </h1>
      <span class="updated">{formatDate(lastUpdated)}</span>
    </div>
  </div>
</header>

<style>
  .header {
    background: linear-gradient(135deg, #ea580c 0%, #f97316 50%, #fb923c 100%);
    color: white;
    padding: 16px;
  }

  @media (min-width: 640px) {
    .header {
      padding: 20px 24px;
    }
  }

  .header-content {
    max-width: 1200px;
    margin: 0 auto;
  }

  .header-main {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
  }

  h1 {
    font-size: 1.25rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 10px;
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  @media (min-width: 640px) {
    h1 {
      font-size: 1.5rem;
    }
  }

  .logo {
    font-size: 1.5rem;
  }

  @media (min-width: 640px) {
    .logo {
      font-size: 1.75rem;
    }
  }

  .updated {
    font-size: 0.65rem;
    font-weight: 500;
    opacity: 0.9;
    white-space: nowrap;
    padding: 4px 10px;
    background: rgba(255,255,255,0.2);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-radius: var(--radius-full);
  }
</style>
