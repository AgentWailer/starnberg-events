---
interface Props {
  station?: string;
  stationCode?: string;
}

const { station = "Possenhofen", stationCode = "MPH" } = Astro.props;
const dbUrl = `https://iris.noncd.db.de/wbt/js/index.html?bhf=${stationCode}`;
const mobileUrl = `https://reiseauskunft.bahn.de/bin/bhftafel.exe/dn?input=${station}&boardType=dep&time=actual&productsFilter=1111111111&start=yes`;
---

<section class="train-info">
  <div class="train-content">
    <div class="train-icon">🚆</div>
    <div class="train-text">
      <h2>S-Bahn {station}</h2>
      <p>Live-Abfahrten der S6</p>
    </div>
    <div class="train-actions">
      <a href={dbUrl} target="_blank" rel="noopener" class="train-btn primary">
        Abfahrten ansehen →
      </a>
      <a href={mobileUrl} target="_blank" rel="noopener" class="train-btn secondary">
        DB Navigator
      </a>
    </div>
  </div>
  
  <div class="train-schedule">
    <div class="schedule-item">
      <span class="line">S6</span>
      <span class="dest">→ Tutzing</span>
      <span class="freq">alle 20 Min</span>
    </div>
    <div class="schedule-item">
      <span class="line">S6</span>
      <span class="dest">→ München</span>
      <span class="freq">alle 20 Min</span>
    </div>
  </div>
</section>

<style>
  .train-info {
    background: linear-gradient(135deg, #1a365d 0%, #2c5282 100%);
    color: white;
    border-radius: 12px;
    padding: 1.25rem;
    margin-bottom: 1.5rem;
  }

  .train-content {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .train-icon {
    font-size: 2rem;
    background: rgba(255,255,255,0.15);
    padding: 0.5rem;
    border-radius: 8px;
  }

  .train-text {
    flex: 1;
    min-width: 150px;
  }

  .train-text h2 {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
  }

  .train-text p {
    font-size: 0.85rem;
    opacity: 0.8;
    margin: 0.25rem 0 0;
  }

  .train-actions {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .train-btn {
    padding: 0.5rem 1rem;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s;
  }

  .train-btn.primary {
    background: white;
    color: #1a365d;
  }

  .train-btn.primary:hover {
    background: #e2e8f0;
    text-decoration: none;
  }

  .train-btn.secondary {
    background: rgba(255,255,255,0.15);
    color: white;
  }

  .train-btn.secondary:hover {
    background: rgba(255,255,255,0.25);
    text-decoration: none;
  }

  .train-schedule {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(255,255,255,0.2);
    flex-wrap: wrap;
  }

  .schedule-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
  }

  .line {
    background: #38a169;
    color: white;
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
    font-weight: 600;
    font-size: 0.75rem;
  }

  .dest {
    font-weight: 500;
  }

  .freq {
    opacity: 0.7;
    font-size: 0.8rem;
  }

  @media (max-width: 500px) {
    .train-content {
      flex-direction: column;
      text-align: center;
    }

    .train-actions {
      width: 100%;
      justify-content: center;
    }
  }
</style>
