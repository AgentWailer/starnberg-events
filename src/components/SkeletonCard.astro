---
/**
 * SkeletonCard - Loading placeholder for EventCard
 * 
 * UX Research Basis:
 * - NNGroup "Skeleton Screens 101": Shows page structure while loading
 * - Reduces perceived wait time
 * - Prevents layout shift (CLS) by matching final dimensions
 * - Shimmer animation indicates progress (respects prefers-reduced-motion)
 */
---

<article class="skeleton-card" aria-hidden="true" role="presentation">
  <div class="skeleton-bar"></div>
  <div class="skeleton-content">
    <div class="skeleton-meta">
      <div class="skeleton skeleton-location"></div>
    </div>
    <div class="skeleton skeleton-title"></div>
    <div class="skeleton skeleton-title-short"></div>
    <div class="skeleton-info">
      <div class="skeleton skeleton-date"></div>
      <div class="skeleton skeleton-category"></div>
    </div>
  </div>
</article>

<style>
  .skeleton-card {
    background: var(--color-card);
    border-radius: var(--radius-sm);
    overflow: hidden;
    border: 1px solid var(--color-border-light);
    /* Match EventCard min-height for CLS prevention */
    min-height: 100px;
  }

  .skeleton-bar {
    height: 4px;
    background: var(--color-border);
  }

  .skeleton-content {
    padding: 16px;
  }

  @media (min-width: 640px) {
    .skeleton-content {
      padding: 20px;
    }
  }

  .skeleton-meta {
    margin-bottom: 10px;
  }

  .skeleton-location {
    width: 120px;
    height: 14px;
  }

  .skeleton-title {
    width: 100%;
    height: 18px;
    margin-bottom: 6px;
  }

  .skeleton-title-short {
    width: 65%;
    height: 18px;
    margin-bottom: 14px;
  }

  .skeleton-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .skeleton-date {
    width: 100px;
    height: 14px;
  }

  .skeleton-category {
    width: 70px;
    height: 14px;
  }

  /* Use the global .skeleton class from Layout.astro for shimmer animation */
</style>
